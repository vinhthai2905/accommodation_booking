import { clsx } from "clsx"

export default function HomeIcon() {
    return (
        <a className={clsx(
        )}>
            <span className={clsx(
                "text-white"
            )}>
                Booking
            </span>
        </a>
    )
}